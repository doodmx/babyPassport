<div class='btn-group'>


    @if($type=='pacient')
        <a href="{{route('users.profile',[$id])}}" class="btn btn-teal btn-sm">
          <i class="fas fa-user-circle"></i> Perfil
        </a>
    @endif


</div>

